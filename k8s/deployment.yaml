apiVersion: apps/v1
kind: Deployment
metadata:
  name: streamlit-app
  namespace: streamlit-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: streamlit-app
  template:
    metadata:
      labels:
        app: streamlit-app
    spec:
      containers:
        - name: streamlit-app
          image: saneja1/streamlit-app:25
          ports:
            - containerPort: 8501
